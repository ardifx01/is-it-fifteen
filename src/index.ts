/**
 * Checks if the given number is equal to 15.
 * @param {number} number - The number to be checked.
 * @returns {boolean} - Returns `true` if the number is 15, `false` otherwise.
 */
function isItFifteen(number: number): boolean {
  return number === 15
}

export default isItFifteen
